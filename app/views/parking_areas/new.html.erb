<%= turbo_frame_tag "modal" do %>
  <div class="fixed inset-0 z-50 flex items-center justify-center bg-gray-900/50 backdrop-blur-sm transition-all">
    
    <div class="bg-white rounded-2xl shadow-2xl sm:max-w-lg sm:w-full overflow-hidden border border-white/20">
      
      <div class="px-6 py-5 border-b border-gray-100 bg-indigo-50/80">
        <div class="flex items-center space-x-3">
          <div class="p-2 bg-indigo-600 rounded-lg shadow-lg shadow-indigo-200">
            <svg class="h-5 w-5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
            </svg>
          </div>
          <h3 class="text-xl font-bold text-indigo-900">新しいエリアを登録</h3>
        </div>
      </div>

      <div class="px-8 py-6">
        <div class="mt-2">
          <%= render "form", parking_area: @parking_area %>
        </div>
      </div>

      <div class="px-6 py-5 bg-gray-50/80 border-t border-gray-100 flex justify-center">
        <%= link_to "一覧に戻る", 
                    parking_lot_parking_areas_path(@parking_lot), 
                    data: { turbo_frame: "_top" }, 
                    class: "px-12 py-2.5 bg-white border border-gray-300 rounded-xl text-gray-700 font-bold hover:bg-gray-50 hover:border-gray-400 transition-all shadow-sm active:transform active:scale-95" %>
      </div>

    </div>
  </div>
<% end %>
