<div class="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-extrabold text-gray-900">
      🅿️ 駐車場スペース一覧
    </h1>

    <%# 新規作成ボタン %>
    <%= link_to '➕ 新しい駐車場スペースを登録', new_parking_lot_parking_space_path(@parking_lot),
    class: 'inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500' %>
  </div>

  <div class="bg-white shadow overflow-hidden sm:rounded-lg">
    <% if @parking_spaces.any? %>

      <%# テーブルヘッダー (PCビューのみ表示) %>
      <div class="hidden sm:block">
        <div class="px-6 py-3 text-xs font-medium text-gray-500 uppercase tracking-wider grid grid-cols-6 gap-4 border-b border-gray-200">
          <div class="col-span-2">駐車場スペース名</div>
          <div class="col-span-1">横幅 (m)</div>
          <div class="col-span-1">長さ (m)</div>
          <div class="col-span-2">契約者</div>
        </div>
      </div>

      <%# 駐車場リスト %>
      <ul role="list" class="divide-y divide-gray-200">
        <% @parking_spaces.each do |parking_space| %>
          <li class="p-4 sm:px-6 hover:bg-gray-50 transition duration-150 ease-in-out">
            <div class="grid grid-cols-6 gap-4 items-center"> 

              <%# 1. 駐車場スペース名 (全画面で表示) %>
              <div class="col-span-6 sm:col-span-2 text-sm font-medium text-indigo-600 truncate">
                <span class="sm:hidden text-gray-500 font-semibold mr-2">名前:</span>
                <%= link_to parking_space.name, [@parking_lot, parking_space] %> 
              </div>

              <%# 2. 横幅 (PCではcol-span-1, スマホでは新しい行のcol-span-3) %>
              <div class="col-span-3 sm:col-span-1 text-sm text-gray-500 truncate">
                <span class="sm:hidden text-gray-500 font-semibold mr-2">横幅:</span>
                <%= parking_space.width.present? ? "#{parking_space.width} m" : '---' %>
              </div>

              <%# 3. 長さ (PCではcol-span-1, スマホでは新しい行のcol-span-3) %>
              <div class="col-span-3 sm:col-span-1 text-sm text-gray-500 truncate">
                <span class="sm:hidden text-gray-500 font-semibold mr-2">長さ:</span>
                <%= parking_space.length.present? ? "#{parking_space.length} m" : '---' %>
              </div> 
              <%# 4. 契約者名を表示するカラム (PCではcol-span-2) %>
              <div class="col-span-6 sm:col-span-2 text-sm text-gray-500">
                <span class="sm:hidden text-gray-500 font-semibold mr-2">契約者:</span>

                <%# 現在有効な契約を取得（終了日が未来、または設定されていないもの） %>
                <% active_contract = parking_space.contract_parking_spaces.find_by(end_date: ::ACTIVE_CONTRACT_END_DATE) %>

                <% if active_contract %>
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                    👤 <%= active_contract.contractor.first_name %><%= active_contract.contractor.last_name %>
                  </span>
                <% else %>
                  <span class="text-gray-400 italic">空き</span>
                <% end %>
              </div>
            </div>
          </li>
        <% end %>
      </ul>

    <% else %>
      <%# 駐車場スペースが登録されていない場合 %>
      <div class="p-8 text-center text-gray-500">
        <p class="text-lg mb-2">まだ駐車場スペースが登録されていません。</p>
        <%= link_to '最初のスペースを登録する', new_parking_lot_parking_space_path(@parking_lot),
          class: 'text-indigo-600 hover:text-indigo-700 font-medium' %>
      </div>
    <% end %>
  </div>
</div>
