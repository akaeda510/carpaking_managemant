<div class="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-6 gap-4">
    <h1 class="text-2xl sm:text-3xl font-extrabold text-gray-900">
      <%= @parking_lot.name %> 
    </h1>

    <%= link_to '➕ 駐車場を登録', new_parking_lot_parking_space_path(@parking_lot),
      class: 'w-full sm:w-auto inline-flex justify-center items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700' %>
  </div>

  <div class="bg-white shadow overflow-hidden rounded-lg">
    <% if @parking_spaces.any? %>

      <div class="hidden sm:block">
        <div class="px-6 py-3 text-xs font-medium text-gray-500 uppercase tracking-wider grid grid-cols-8 gap-2 border-b border-gray-200 bg-gray-50">
          <div class="col-span-2">駐車番号</div>
          <div class="col-span-2 text-center">駐車タイプ</div>
          <div class="col-span-1 text-center">ステータス</div>
          <div class="col-span-1 text-center">契約者</div>
      <div class="col-span-2 text-right">アクション</div>
        </div>
      </div>

      <% @parking_spaces.each do |parking_space| %>
        <li class="p-4 sm:px-6 hover:bg-gray-50 transition">
          <div class="flex flex-col sm:grid sm:grid-cols-8 gap-2 sm:gap-2 items-start sm:items-center"> 

            <div class="flex sm:block sm:col-span-2 text-sm">
              <span class="sm:hidden w-32 flex-shrink-0 text-gray-500 font-semibold">名前:</span>
              <span class="font-bold text-gray-900"><%= parking_space.name %></span>
            </div>

            <div class="flex sm:block sm:col-span-2 text-sm sm:text-center">
              <span class="sm:hidden w-32 flex-shrink-0 text-gray-500 font-semibold">駐車タイプ:</span>
              <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-blue-50 text-blue-700 border border-blue-200 whitespace-nowrap">
                <%= parking_space.parking_type_i18n %>
              </span>
            </div>

            <div class="flex sm:block sm:col-span-1 text-sm sm:text-center">
              <span class="sm:hidden w-32 flex-shrink-0 text-gray-500 font-semibold">ステータス:</span>
              <%= parking_space.status_badge %>
            </div>
            <div class="flex sm:block sm:col-span-1 text-sm sm:text-center">
              <span class="sm:hidden w-32 flex-shrink-0 text-gray-500 font-semibold">契約者:</span>
              <% active_contract = parking_space.contract_parking_spaces.find_by(end_date: ::ACTIVE_CONTRACT_END_DATE) %>
              <% if active_contract %>
                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                  👤 <%= active_contract.contractor.first_name %>
                </span>
              <% else %>
                <span class="text-gray-400 italic text-xs">なし</span>
              <% end %>
            </div>

            <div class="flex sm:block sm:col-span-2 text-sm sm:text-right w-full mt-2 sm:mt-0 border-t sm:border-none pt-2 sm:pt-0">
              <span class="sm:hidden w-32 flex-shrink-0 text-gray-500 font-semibold">アクション:</span>
              <div class="flex space-x-4 sm:justify-end">
                <%= link_to '詳細', [@parking_lot, parking_space], class: "text-indigo-600 hover:text-indigo-900 font-medium whitespace-nowrap" %>
                <%= link_to '削除', [@parking_lot, parking_space], 
                  data: { turbo_method: :delete, turbo_confirm: '本当に削除しますか？' }, 
                  class: "text-red-600 hover:text-red-900 font-medium whitespace-nowrap" %>
              </div>
            </div>

          </div>
        </li>
      <% end %>
      </ul>
    <% else %>

      <div class="p-8 text-center text-gray-500">
        <p class="text-lg mb-2">まだ駐車場スペースが登録されていません。</p>
        <%= link_to '最初のスペースを登録する', new_parking_lot_parking_space_path(@parking_lot),
          class: 'text-indigo-600 hover:text-indigo-700 font-medium' %>
      </div>
    <% end %>
  </div>
</div>
