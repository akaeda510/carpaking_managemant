<div class="max-w-5xl mx-auto p-6">
  <nav class="mb-6 text-sm text-gray-500">
    <%= link_to "区画詳細へ戻る", admin_parking_space_path(@parking_space), class: "hover:underline" %>
  </nav>

  <h1 class="text-2xl font-bold text-gray-800 mb-6">
    区画 <%= @parking_space.name %>：契約履歴一覧
  </h1>

  <%# ここでテーブルを表示 %>
  <div class="bg-white shadow rounded-lg overflow-hidden">
    <%= render 'admin/parking_spaces/space_contract_history', contract_histories: @contracts %>
  </div>
</div>
