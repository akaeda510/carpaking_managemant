<!DOCTYPE html>
<html>
  <head>
    <title>管理画面 | Myapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# 管理画面専用のCSSがあればここで読み込み %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turb-track": "relod", type: "module" %>
  </head>

  <body style="display: flex; min-height: 100vh; margin: 0;">
    <%# サイドバー（共通メニュー） %>
    <% if admin_signed_in? %>
      <aside style="width: 250px; background: #343a40; color: white; padding: 20px;">
        <h2>管理メニュー</h2>
        <ul style="list-style: none; padding: 0;">
          <li style="margin-bottom: 15px;"><%= link_to "ダッシュボード", admin_root_path, style: "color: white; text-decoration: none;" %></li>
          <li style="margin-bottom: 15px;"><%= link_to "駐車場管理 (予定)", "#", style: "color: #ccc; text-decoration: none;" %></li>
          <hr style="border-color: #444;">
          <li><%= link_to "ログアウト", destroy_admin_session_path, data: { turbo_method: :delete }, style: "color: #ff8888; text-decoration: none;" %></li>
        </ul>
      </aside>
    <% end %>

    <%# メインコンテンツ %>
    <main style="flex-grow: 1; padding: 20px; background: #f8f9fa;">
      <% if admin_signed_in? %>
        <header style="margin-bottom: 20px; border-bottom: 1px solid #ddd;">
          <p>ログイン中: <%= current_admin.email %> (ID: <%= current_admin.id %>)</p>
        </header>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>

